# Angry Data Scanner - New Website

Новая версия сайта Angry Data Scanner, разработанная в стиле Datadog с применением best practices.

## Особенности

- 🎨 Современный дизайн в стиле Datadog
- 🌓 Поддержка темной и светлой темы
- 📱 Полностью адаптивный дизайн
- ⚡ Плавные анимации и переходы
- 🔍 Структурированный контент с таблицами
- 🎯 Интуитивная навигация
- 🏗️ Модульная архитектура кода
- 📝 Легко редактируемая структура

## Структура проекта

```
new_version/
├── index.html          # Главная страница (семантическая разметка)
├── css/
│   └── styles.css      # Стили с CSS переменными и компонентной организацией
├── js/
│   ├── script.js       # Модульный JavaScript с разделением на компоненты
│   └── config.js       # Конфигурационный файл со всеми данными
└── README.md           # Документация
```

### Организация файлов

- **`css/`** - Все стили проекта
- **`js/`** - Все JavaScript файлы (скрипты и конфигурация)
- **Корень** - HTML файлы и документация

## Архитектура кода

### CSS (styles.css)
- **CSS Variables**: Все цвета, отступы, размеры вынесены в переменные
- **Компонентная организация**: Стили организованы по логическим секциям
- **Темная/светлая тема**: Легко переключается через data-атрибут
- **Responsive**: Медиа-запросы для всех размеров экранов

### JavaScript (script.js)
- **Модульная структура**: Код разделен на независимые модули
  - `ThemeManager` - управление темой
  - `ScrollManager` - управление прокруткой
  - `AnimationManager` - анимации при скролле
  - `TableEnhancement` - улучшения таблиц
  - `Utils` - вспомогательные функции
- **Константы**: Все магические числа вынесены в константы
- **Обработка ошибок**: Try-catch блоки для безопасной инициализации

### Config (config.js)
- **Централизованные данные**: Все данные сайта в одном месте
- **Легко редактировать**: Изменение контента без правки HTML
- **Структурированный формат**: JSON-like структура для удобства

### HTML (index.html)
- **Семантическая разметка**: Использование правильных HTML5 тегов
- **Комментарии**: Структура разделена комментариями
- **Доступность**: ARIA-атрибуты где необходимо

## Использование

Просто откройте `index.html` в браузере. Все стили и скрипты подключены локально.

### Локальный сервер (рекомендуется)

Для лучшей работы рекомендуется использовать локальный сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (http-server)
npx http-server

# PHP
php -S localhost:8000
```

Затем откройте `http://localhost:8000` в браузере.

## Функциональность

- **Переключение темы**: Кнопка в правом верхнем углу навигации
- **Плавная прокрутка**: Автоматическая прокрутка к секциям при клике на ссылки
- **Анимации**: Плавное появление элементов при прокрутке
- **Адаптивность**: Оптимизация для мобильных устройств, планшетов и десктопов

## Технологии

- HTML5
- CSS3 (CSS Variables для тем)
- Vanilla JavaScript
- Google Fonts (Inter)

## Браузерная поддержка

- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

